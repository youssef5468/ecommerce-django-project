# No database model here on purpose — the cart lives entirely in the
# session (see cart.py). This file is kept so `cart` stays a normal
# Django app (admin.py/apps.py expect it), but intentionally defines
# nothing.
